node_modules/
# Build output stays out of master. GitHub Actions builds it and force-pushes
# the result to the `deploy` branch, which is what cPanel checks out.
dist/
kinnav-site.zip
.DS_Store
*.local
public/api/.state/
src/test/.state/
