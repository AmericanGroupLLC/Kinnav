Sign-in no longer hangs when you have no connection. It now gives up quickly and
tells you the network is the problem, instead of leaving a spinner running.

Early access: the guardian network is still being built out, so guardian
profiles are samples and Safe Call is labelled in-app as a simulation.
